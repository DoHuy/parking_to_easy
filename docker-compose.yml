version: '3'
# start by cleaning up old containers since logger will conflict
# docker-compose rm -f;docker-compose up
services:
  db:
    image: mysql:latest
    container_name: parking_db
    volumes:
      - ./resource/migration:/var/lib/mysql
    networks:
      - backend
  redis:
    image: redis:latest
    networks:
      - backend
  api:
    build: .
    ports:
      - "8124:8085"
    networks:
      - backend
    depends_on: #cho db start trc
      - db
      - redis
networks:
  backend:
    driver: bridge
